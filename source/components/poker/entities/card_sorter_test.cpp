#include "components/poker/entities/card_def.h"

#include <iostream>
#include <unordered_set>

#include "gtest/gtest.h"

namespace poker {
namespace statistics {

TEST(CardSorterTest, MultiSort) {
/*
  std::vector<Card> cards;
  cards.push_back(UniqueIdToCard(1));
  cards.push_back(UniqueIdToCard(2));
  cards.push_back(UniqueIdToCard(3));
  cards.push_back(UniqueIdToCard(4));
  cards.push_back(UniqueIdToCard(5));

  EXPECT_EQ(102030405, CardsToUniqueId(cards));

  cards.clear();
  cards.push_back(UniqueIdToCard(10));
  cards.push_back(UniqueIdToCard(2));
  cards.push_back(UniqueIdToCard(3));
  cards.push_back(UniqueIdToCard(4));
  cards.push_back(UniqueIdToCard(5));

  EXPECT_EQ(203040510, CardsToUniqueId(cards));

  cards.clear();
  cards.push_back(UniqueIdToCard(1));
  cards.push_back(UniqueIdToCard(20));
  cards.push_back(UniqueIdToCard(3));
  cards.push_back(UniqueIdToCard(4));
  cards.push_back(UniqueIdToCard(5));

  EXPECT_EQ(103040520, CardsToUniqueId(cards));
*/
}

}  // namespace statistics
}  // namespace poker
